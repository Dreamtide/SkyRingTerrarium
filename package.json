{
  "name": "dream-game",
  "private": true,
  "version": "0.1.0",
  "description": "D.R.E.A.M. - Dogs Ride Evolving Attack Mechs",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --workspace=@dream/server",
    "dev:client": "npm run dev --workspace=@dream/client",
    "build": "npm run build --workspace=@dream/shared && npm run build --workspace=@dream/server && npm run build --workspace=@dream/client",
    "build:shared": "npm run build --workspace=@dream/shared",
    "typecheck": "npm run typecheck --workspace=@dream/shared && npm run typecheck --workspace=@dream/server && npm run typecheck --workspace=@dream/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
